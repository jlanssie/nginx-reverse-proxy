server {
        # Ports
        listen 3000;
        listen [::]:3000;
        
        # Server name
        server_name mydomain.com;
        
        # Return the request with permanent redirection to HTTPS version
        return 301 https://$host$request_uri;   
}